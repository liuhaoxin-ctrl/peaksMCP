# Optional runtime state directory. Ports, hosts and modes belong in YAML profiles.
PEAKSMCP_HOME=/absolute/path/to/.peaksMCP

# Optional Claude Plugin override when peaksMCP is outside PATH.
PEAKSMCP_EXECUTABLE=/absolute/path/to/env/bin/peaksMCP
